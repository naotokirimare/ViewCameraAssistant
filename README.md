# ViewCamera Assistant v1.0α99

複数ファイル構成へ移行したモバイルUI改善版です。

## 変更点

- iPhoneで2D図と情報カードが重ならないように修正
- 情報カードを横スクロール化
- 3DのXYZ軸を右上へ移動・縮小
- 3Dにピンチズーム追加
- セーフエリア対応
- α99の測定モードを維持
- 今後の開発のために CSS / JS を分割

## 構成

```text
index.html
css/style.css
js/state.js
js/optics.js
js/draw2d.js
js/draw3d.js
js/measure.js
js/app.js
assets/
```

GitHub Desktopでは、このフォルダごと上書きして Commit → Push してください。


## v1.0α99 バグ修正

- 横断面が起動直後に表示されない問題を修正
- 測定開始ボタンが反応しない問題を修正
- 測定開始/停止を1つのトグルボタンとして再実装
- ゼロ補正リセットボタンを確実に表示
- おすすめ運用欄を削除
- 撮影画面の測定ボタンを維持


## v1.0α99 確認済み修正

- 「商品」表記を「被写体」へ変更
- ピント面と被写体面が重なった時、ピント面が太くなる
- レンズ面 / センサー面のTilt・Swing可動範囲を±90°へ拡張
- ZIPをリポジトリ直下へ上書きしやすいフラット構成で作成


## v1.0α99 修正

- 重なり時のピント面強調を細めに調整
- iPhoneでも重なり時にピント面が太くなるよう、表示更新処理を強化
- レンズ面 / カメラ / センサー面のTilt・Swing操作上限を±90°へ修正


## v1.0α99 変更

- 横画面対応はいったん保留
- α99ベースへ戻して、撮影 / 3D / 設定 / 測定の空白問題を優先修正
- 微調整パネルを廃止
- α99の太い透明タッチ判定を再実装
- レンズ面 / センサー面 / カメラ軸のラインを長くしてドラッグしやすく変更
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 測定モードの「測定停止」ボタン表示崩れを修正
- 測定中は赤系の大きな停止ボタンとして表示
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 設定画面のレンズ選択を2段選択へ変更
  - レンズメーカー
  - レンズシリーズ
  - 焦点距離
- Schneider / Rodenstock / Fujinon / Nikon / Canon / Kodak / Goerz / Caltar / Wollensak / Konica / Cooke / その他 を追加
- UIレイアウトは極力触らず、設定データ中心の安全な更新
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 測定基準を追加
  - 背面垂直（推奨・コールドシュー固定）をデフォルト
  - 背面水平（机上測定）へ切り替え可能
- ゼロ補正は維持
  - カメラアングル決定後にゼロ補正して、Front/Rearの相対Tilt/Swingを測る運用に対応
- 測定状態表示に「垂直基準 / 水平基準」を追加
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 背面垂直モードのSwing測定を修正
- 垂直基準ではSwingに `alpha`（方位/yaw）を使用
- 水平基準では従来通り `gamma` を使用
- Swing値の0/360°またぎを補正
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 測定軸マッピングを修正
  - 背面水平: Tilt = スマホ短辺 / Swing = α軸（コンパス方向）
  - 背面垂直: Tilt = スマホ短辺（±90°補正） / Swing = スマホ長辺の回転
- Swingの0/360°またぎ補正は維持
- ゼロ補正は維持
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- Swing方向を修正
  - 背面水平: Tilt = beta / Swing = -alpha
  - 背面垂直: Tilt = beta±90°補正 / Swing = -alpha±90°補正
- ゼロ補正は維持
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 背面垂直モードのSwing ±90°補正を修正
- `-alpha` を一度 -180〜180°に正規化してから ±90°補正するよう変更
- 背面水平の挙動は維持
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 背面垂直モードのSwing補正を修正
- `-alpha` の数値を後から±90°補正する方式をやめ、Swing軸そのものを垂直方向へ90°回転させる方式へ変更
- 背面水平の挙動は維持
- ゼロ補正は維持
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 背面垂直モードのSwing軸を再修正
- `-alpha + 90°` のような数値オフセットをやめ、垂直時の長辺軸回転として `-(alpha + gamma)` を使用
- 背面水平の挙動は維持
- ゼロ補正は維持
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 基準値保存機能を追加
  - 基準保存
  - 基準をゼロにする
  - 基準クリア
- ゼロ補正とは別に、撮影開始時の正対状態を基準値として保持
- 保存した基準値との差分表示を追加
- UI変更は測定画面内に限定
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 測定画面の順番を変更
  - 測定基準
  - 測定対象
- 文言を調整
  - 基準保存 → Camera基準を保存
  - 基準をゼロにする → 基準値から計測
  - 基準クリア → 基準値クリア
  - 現在値を反映 → 測定値を反映
- 測定対象に応じて「反映先：Camera / Front / Rear / 被写体面」を表示
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 設定画面に「撮影倍率」を追加
- 焦点距離と蛇腹長から撮影倍率を自動計算
  - 撮影倍率 = 蛇腹長 ÷ 焦点距離 − 1
- レンズや焦点距離、蛇腹長を変更すると自動更新
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 測定対象の順番を撮影準備フローに合わせて変更
  1. 読むだけ
  2. 被写体面
  3. カメラ角度
  4. レンズ面（Front）
  5. センサー面（Rear）
- 基準値項目の文言を変更
  - 現在値をCamera基準として保存
  - Camera基準から計測
  - 基準値クリア
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 測定基準に「背面垂直・横画面」を追加
- 従来の背面垂直は「背面垂直・縦画面」として維持
- 背面垂直・横画面ではTilt/Swingの入力軸を90°入れ替える試作
- 背面水平の挙動は維持
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 背面垂直・横画面の軸入れ替えを修正
  - 縦画面時のTilt → 横画面時のSwing
  - 縦画面時のSwing → 横画面時のTilt
- 「背面垂直・縦画面」から（推奨）表記を削除
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 背面垂直の縦画面 / 横画面選択を廃止し、画面向きを自動判定
- 背面垂直では、iPhoneが縦画面なら従来の縦基準、横画面ならTilt/Swingを自動で入れ替え
- 測定基準は「背面垂直」「背面水平」の2つに整理
- 背面水平の挙動は維持
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 背面垂直・横画面時のSwing挙動を修正
- 横画面時のTiltはα99の正常動作を維持
- 横画面時のSwingは、スマホを左右に振る（方位を変える）動きで変化するよう `-alpha` 系へ変更
- 背面垂直・縦画面、背面水平の挙動は維持
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- α99の回転行列ベース測定を取り消し
- 測定ロジックをα99相当に戻して、縦画面の正常動作を復旧
- UIと設定項目は維持
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 測定モード上部に案内文を追加
  - 📱 縦画面で固定して測定してください。
- 横画面対応は保留し、縦画面固定での実用運用を明確化
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 測定モードの「測定開始 / 測定停止」ボタン位置を変更
- 「測定値を反映」ボタン群の下へ移動
- 動作・文言・デザインは変更なし
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- Camera基準とゼロ補正を分離
- Camera基準との差分表示はゼロ補正の影響を受けないよう修正
- Camera基準はraw測定値を保存
- ゼロ補正はFront / Rear測定用の一時基準として独立
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 「Camera基準から計測」を共通機能として整理
- 文言を「Camera基準から相対計測」に変更
- Camera / 被写体面 / Front / Rear すべてで、保存したCamera基準を0°として測定・反映できるよう意図を明確化
- 動作ロジックはα99の基準値/ゼロ補正分離を維持
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 3Dモードの「俯瞰」ボタンを修正
- 真下から見上げる表示ではなく、真上から見下ろす表示へ変更
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 3Dモードの俯瞰表示の向きを修正
- 2D俯瞰表示と同じ並びになるよう、真上からの表示を180°回転
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 「現在値をCamera基準として保存」で、基準値カードに表示される値が現在表示中の測定値とズレる問題を修正
- 内部的にはraw測定値を保存し、Camera基準差分とCamera基準から相対計測はゼロ補正の影響を受けないまま維持
- 表示用の基準値と計算用の基準値を分離
- キャッシュ対策を `?v=99` に更新

## v1.0α99 計算エンジン修正
- 旧ピント面近似式 `camera + front*1.15 + rear*0.75` を廃止。
- 焦点距離・蛇腹長から薄レンズ式で被写体側距離を計算。
- レンズ面とセンサー面のScheimpflug交点を使ってピント面角度を描画。
- 蛇腹長入力がピント面表示に反映されるように修正。

実機検証基準: 180mm / 蛇腹220mm / 被写体Tilt約-29.9° / カメラTilt約-6.4°。


## v1.0α99 変更

- 設定モードに「蛇腹長補正 mm」を追加
- 実効蛇腹長 = 蛇腹長 + 蛇腹長補正
- 撮影倍率計算に実効蛇腹長を使用
- GFXマウントやアダプター分の補正に対応
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 蛇腹長補正が撮影倍率計算に反映されない問題を修正
- 撮影倍率計算を `実効蛇腹長 = 蛇腹長 + 蛇腹長補正` に統一
- 焦点距離より短い場合も0に丸めず、そのまま倍率を表示
- キャッシュ対策を `?v=99` に更新


## v1.0α99 変更

- 蛇腹長補正が撮影倍率表示だけでなく、Scheimpflug/ピント面計算側にも反映されるよう修正
- `opticsDistances()` を実効蛇腹長ベースに統一
- 実効蛇腹長 = 蛇腹長 + 蛇腹長補正
- α99で残っていた raw 蛇腹長参照を修正
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- α99のRear continuity fixを取り消し
- Rearが25°付近で合う表示になる問題を回避
- 計算・蛇腹長補正はα99相当に戻す
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- Rear角度が1.4°付近から3.4°付近へ飛ぶ問題を修正
- 計算式には触れず、Front / Rear / 被写体面のドラッグ角度だけを方向線として扱うよう変更
- 線は180°反転しても同じ面なので、近い方の角度枝を選ぶ処理を追加
- α99のような計算変更や強制補正は入れず、α99/α99相当の計算精度を維持
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- ピント面が被写体面に重なる付近で角度枝が切り替わって飛ぶ問題を修正
- ピント面 / 被写体面は180°反転しても同じ面として扱うよう変更
- ピント面角度は被写体面に近い方の角度枝を選択
- 重なり判定と差分表示も180°方向線として計算
- α99のセンサー平滑化は使わず、根本原因のピント面角度枝を修正
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- α99の直前値追従アンラップ方式を取り消し
- 0°付近で飛び幅が大きくなる問題を回避
- 枝選択は行わず、0°付近の明らかな一瞬のスパイクだけを拒否する方式へ変更
- 光学計算・ゼロ補正・ピント面枝修正は変更なし
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- Tiltが0°付近をまたぐとピント面が別解へ飛ぶ問題への対策
- レンズ面とセンサー面がほぼ平行になる近傍では、ピント面をカメラ面側へ連続補正
- 3°以上離れた通常域では従来のScheimpflug計算を維持
- センサー値やゼロ補正は変更なし
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- Tilt 0°付近のリアルタイム測定飛び対策
- Tiltだけ測定開始時の生Tiltを内部基準として固定
- Swing、ゼロ補正、光学計算式は変更なし
- 光学計算へは開始基準からの相対Tiltとして渡すため、ピント面の物理角度は維持
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- 測定モードにデバッグ表示を追加
- alpha / beta / gamma をリアルタイム表示
- mapped Tilt / mapped Swing、raw Tilt / raw Swing、zero補正後Tilt/Swingを表示
- Camera / Front / Rear / 被写体面の現在値を表示
- 飛んだ瞬間にどの値が変化したか見やすいよう、変化監視を追加
- 計算式・測定補正は変更なし
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- 測定Debugに focus Angle / focus Δ / Scheimpflug交点 X,Y を追加
- 変化監視に focus / focusDiff を追加
- 計算式・補正ロジックは変更なし
- `?v=99`


## v1α99 変更

- Safariから離れて戻った時のDeviceOrientation再初期化対策
- Safariを離れた時点で測定を自動停止
- 復帰後は「測定開始」を押してセンサー基準を取り直す運用に変更
- リアルタイムONも自動OFF
- Debug表示はα99のまま維持
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- 大きな変化が起きた瞬間のDebug値を自動記録する Jump Capture を追加
- beta / mapped Tilt / raw Tilt / display Tilt / Front / Rear / focus Angle / focus Δ を監視
- 急変を検知すると、その瞬間の値をPNG画像として画面内に生成
- 画像は長押し保存、または画面スクショで記録可能
- クリアボタンで次の急変を再記録可能
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- Jump CaptureをFlight Recorder方式に変更
- 直前18フレームをリングバッファで保持
- 異常検知時に、飛ぶ前から飛んだ瞬間までを1枚のPNGにまとめて記録
- 通常の3〜4°程度の手ブレ/動きでは記録しないよう、検知条件を強化
- Front / Rear / focus Angle / focus Δ の大きな変化、またはdisplay Tiltとの大きなズレを記録
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- Flight Recorderの検知条件をTilt 0°付近専用に変更
- displayTiltが-2.2°〜+2.2°付近の時だけ監視
- 1フレームで1.0°以上変化した場合に記録
- 直前30フレームをPNGに保存
- 0°付近で起きる1〜2°の飛びを狙って捕捉
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- Tilt 0°付近の短時間平均表示を追加
- displayTilt が ±2.2°以内の時だけ平均を蓄積
- 8フレーム以上かつ平均が±0.35°以内なら「0°安定」と表示
- 光学計算・測定値反映は丸めず、実測Tiltをそのまま使用
- Debugに Tilt avg / 0°判定 を追加
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- Tilt 0°付近の表示用ヒステリシスを追加
- ±0.8°未満では0°側を維持
- +側/−側の切り替えにしきい値を持たせ、0°付近のパタつきを抑制
- 光学計算・測定値反映は丸めず実測Tiltをそのまま使用
- Debugに Tilt hyst / hyst side を追加
- キャッシュ対策を `?v=99` に更新


## v1α99 変更

- Tilt 0°付近の表示用ヒステリシス幅を±1.2°に拡大
- +側/−側の切り替えしきい値を±1.2°へ変更
- ±0.7°程度の揺れで図がパタつきにくくなるよう調整
- 光学計算・測定値反映は丸めず実測Tiltをそのまま使用
- キャッシュ対策を `?v=99` に更新
